-For each button on the elevator:
  -Compare to floor number you want to go to:
    -If it matches the floor number you want to go to:
      -Press the button.
    -If it does not match the floor number you want to go to:
      -Proceed to next button.
    -Continue until floor number desired and button number match.

*Release 1: Waking Up*
-Wait until it is 08:30:00
-Start alarm sound
-Every time two seconds pass
  -Beep loudly
-Until manually disabled
  -Continue beeping every two seconds
-If not manually disabled
  -Continue beeping every two seconds